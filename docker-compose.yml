version: '3.7'

services:
  web:
    build: ./ChinaBack
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./ChinaBack/:/usr/src/ChinaBack/
    ports:
      - 8000:8000
    env_file:
      - ./.env.dev